-record('and', {
    vars=[] :: [clause()]
}).

-record('or', {
    vars=[] :: [clause()]
}).

-record('not', {
    var=0 :: clause()
}).

%% ----------------------------------------------------------------- %%

-type clause() :: 0 | 1 | atom() | #'and'{} | #'or'{} | #'not'{}.

%% ----------------------------------------------------------------- %%
